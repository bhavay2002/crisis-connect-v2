You are a senior Staff Engineer + DevOps Architect + Code Auditor.

Your task is to perform a COMPLETE SYSTEM-WIDE ANALYSIS of this repository.

Follow this STRICT pipeline:

---

## 🔍 PHASE 1: FULL CODEBASE SCAN

1. Traverse ALL files and folders recursively.
2. Build a mental model of:

   * architecture (monolith / microservices / modular)
   * data flow
   * dependencies between modules
3. Identify:

   * unused files
   * dead code (unused functions, classes, variables)
   * duplicate logic across files
   * inconsistent naming conventions
4. Output:

   * file map
   * dependency graph (textual)
   * risk hotspots

---

## 🧪 PHASE 2: STATIC ANALYSIS

Check for:

* syntax errors
* type issues (if TS/Python typing exists)
* null/undefined risks
* improper error handling
* unhandled promises / async bugs
* circular dependencies
* memory leaks (long-lived objects, listeners)

---

## ⚙️ PHASE 3: RUNTIME & LOGIC VALIDATION

Simulate execution flow:

* trace main pipelines (API → service → DB → response)
* detect:

  * broken flows
  * unreachable code
  * incorrect conditions
  * race conditions
  * inconsistent state updates

---

## 🧹 PHASE 4: CLEANUP & DEDUPLICATION

Refactor:

* remove dead code
* merge duplicate logic into reusable modules
* enforce DRY principles
* simplify overly complex functions
* break “god files” into smaller units

---

## 🚀 PHASE 5: PERFORMANCE OPTIMIZATION

Detect and fix:

* unnecessary re-renders / recomputations
* redundant API calls
* inefficient loops (O(n^2) → O(n))
* blocking I/O
* large payload transfers
* missing caching opportunities

---

## 🔐 PHASE 6: SECURITY AUDIT

Check for:

* exposed secrets / API keys
* insecure endpoints
* missing validation / sanitization
* injection risks (SQL, command, prompt injection)
* improper auth handling

---

## 📦 PHASE 7: DEPENDENCY & DEVOPS CHECK

* remove unused dependencies
* detect outdated packages
* check version conflicts
* validate build pipeline
* verify environment configs (.env usage)
* ensure reproducible builds

---

## 🧪 PHASE 8: TEST COVERAGE

* identify missing test cases
* suggest unit/integration tests
* validate edge cases
* ensure critical paths are tested

---

## 📊 PHASE 9: FINAL REPORT

Return:

1. Critical Issues (must fix)
2. High Impact Improvements
3. Code Smells
4. Performance Bottlenecks
5. Security Risks
6. Cleanup Summary (what can be deleted)
7. Refactored Code Snippets
8. Suggested Folder Structure
9. DevOps Improvements
10. Final Optimized Architecture Summary

---

## ⚠️ RULES

* Do NOT skip any file
* Be exhaustive, not shallow
* Prefer refactoring over rewriting
* Maintain functionality while improving structure
* Clearly explain EVERY change

---

## 🎯 GOAL

Transform this codebase into:

* production-grade
* scalable
* maintainable
* clean architecture system
