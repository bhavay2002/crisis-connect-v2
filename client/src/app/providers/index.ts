/**
 * App providers barrel — import all context/provider components from here.
 * Keeps main.tsx clean and provider wiring in one place.
 */
export { WebSocketProvider, useWSContext, useRealtimeMessage } from "@/providers/WebSocketProvider";
